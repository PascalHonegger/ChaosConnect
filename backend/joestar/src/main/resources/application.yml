micronaut:
  application:
    name: joestar
grpc:
  server:
    port: '${joestar.port:`9091`}'
  channels:
    rohan:
      address: '${rohan.server:`localhost`}:${rohan.port:`8081`}'
      plaintext: true
